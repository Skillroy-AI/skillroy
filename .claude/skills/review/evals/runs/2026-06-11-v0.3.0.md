# Eval re-run — review v0.3.0 (publish re-affirmed; gitignore-trap check promoted)
- **Date / runner:** 2026-06-11 / claude-fable-5 (fresh subagent, no authoring context; simulated-interactive per CONVENTIONS §8)
- **Inputs:** fixtures synthesized under /tmp (eval prompts reference non-existent paths); token catalog resolved via ladder step 3 (overlay clone beside the repo); repo untouched (tree was dirty with the v0.3.0 work itself — provenance `60208bc-dirty`).
- **Why re-run:** v0.3.0 promotes the deferred **gitignore-trap** check into `lint-skill.py` (two real-world strikes): walks every `.gitignore` from the skills home to the *repo* root via `git check-ignore -v --no-index`; negation-aware (a deciding `!pattern` is the safe shape, not a trap); runtime/derived dirs (`__pycache__`, venvs) exempt; severity error. CONVENTIONS bumped to v0.5 (rule-table row + §10 bullet).
- **Result:** PASS (4/4 evals, 13/13 expectations)

## Evals (abridged — full evidence in session report)
- Eval 1 (lint a skill, adhoc): phase-scaled severities; unknown token surfaced, never invented; provenance stamped. PASS 4/4.
- Eval 2 (broken YAML + name mismatch): both errors flagged (raw-text fallback), concrete fixes. PASS 3/3.
- Eval 3 (publish readiness): all four bar checks fired at --phase publish; NOT-ready verdict with blockers. PASS 3/3.
- Eval 4 (whole collection): per-skill blocks, errors vs warns, fixes offered. PASS 3/3.

## New-check verification (end-to-end)
1. `--self-test` PASS (clean tree / collection-level trap / safe-pattern clear / monorepo-parent trap).
2. Monorepo-nesting bite: root `.gitignore` `collections/` → `[error] gitignore-trap: .gitignore:1:collections/ matches 'collections/dx-x/.claude/skills' …` (exit 1).
3. Safe `.claude/*` + `!.claude/skills/` pattern → zero trap findings (exit 0; negation-aware confirmed).
4. Hub sweep at promotion time: ops-dfm/ops-gcp/ops-camel/kb-gcp/kb-data/int-bitbucket + skillroy's own skills = zero false positives (the first probe-set draft flagged `__pycache__/` — fixed with the derived-dirs exemption before landing).

## Run notes (maintainer)
1. `skills-ref` not installed Linux-side — eval 3's base-spec step remains "recommend separately" (unchanged limitation).
2. Trap findings attach to the collection block when linting a home, to the skill's findings when linting a single dir — both paths exercised.

Post-run: lint clean; phase stays publish.
