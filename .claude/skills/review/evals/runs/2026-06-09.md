# Eval run — review v0.2.0 (run at adhoc; promoted to publish on pass)
- **Date / runner:** 2026-06-09 / Claude Fable 5 (Claude Code session)
- **Inputs:** fixture skills built per eval scenario (deploy-staging; util with broken YAML +
  name mismatch; release-notes with unbridged scripts/, no evals, no license; a 3-skill collection
  adding good-skill [uninstrumented] and broken-skill [invalid YAML]). No token catalog. Chat-door
  steps performed by the runner following SKILL.md; CLI door = lint-skill.py.
- **Result:** PASS (4/4 evals, 13/13 expectations) — after one fix found by eval 2 (see below)

## Eval 1 — Review the skill in ./my-skills/deploy-staging for compliance
- [x] Runs the linter against the given folder — `lint-skill.py .../my-skills/deploy-staging` executed.
- [x] Findings reported with severities scaled to declared phase — evals/license = `info` at adhoc (would be `error` at publish).
- [x] No errors → states pass-at-phase + what's needed to advance — report: "passes at adhoc; to reach publish add evals/, license (+ run skills-ref)".
- [x] Does not fabricate a canonical token — no catalog supplied; absence of `domain` noted, nothing invented.

## Eval 2 — Broken frontmatter (`description: TODO: fill in`) + name `utils` ≠ folder `util`
- [x] Reports frontmatter invalid YAML from the unquoted colon — linter error pointing at line 3 col 18.
- [x] Reports name ('utils') ≠ folder ('util') — **initially CLI-door gap**: the linter stopped at the
  YAML error and never emitted the name check (the chat door caught it from raw text). Fixed in this
  run: raw-text fallback name check added to lint-skill.py (+ self-test case); re-run emits both errors.
- [x] Both reported as errors with concrete fixes — quote the description; make name equal the folder.

## Eval 3 — Is ./skills/release-notes ready to publish?
- [x] Evaluates against the publish bar — `--phase publish` override used.
- [x] Missing publish-bar items reported as blocking — errors: two-doors, evals, license.
- [x] Clear ready/not-ready verdict with blockers — NOT ready; the three blockers listed with fixes.

## Eval 4 — Lint every skill in ./skills
- [x] Linter run over the whole collection — single invocation covered all three `*/SKILL.md`.
- [x] Per-skill summary distinguishing errors from warns/infos — broken-skill: error; good-skill:
  warn (uninstrumented) + infos; release-notes: warn (two-doors at adhoc) + infos.
- [x] Offers to apply fixes — report ends with the offer per SKILL.md §5.

Post-run: structural lint at publish clean; official `skills-ref validate` (0.1.5 via Windows npx) → Valid.
