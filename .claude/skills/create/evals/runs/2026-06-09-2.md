# Eval run — create v0.2.0 (publish; re-run after the collection capability landed)
- **Date / runner:** 2026-06-09 / Claude Fable 5 (Claude Code session)
- **Inputs:** fixtures under /tmp/skillroy-eval-create2 (evals 5–6 exercise new-collection.py for
  real; eval 1 spot-checked end-to-end with new-skill.py + lint); evals 2–4 acted out against the
  updated SKILL.md (workflow step 0 added; their sections are otherwise unchanged). Simulated user
  turns per CONVENTIONS §8. Version bumped 0.1.0 → 0.2.0 (new-collection.py + workflow step 0), so
  this log supersedes 2026-06-09-reconstructed.md as the current-behaviour evidence.
- **Result:** PASS (6/6 evals, 28/28 expectations)

## Eval 1 — Happy-path skill scaffold (spot-check re-run)
- [x] Folder with SKILL.md + references/ + evals/ stub — scaffolded `smoke-test`, lint clean (one
  expected info: no license, none was given).
- [x] name == folder, kebab-case — `smoke-test`.
- [x] metadata.skillroy with phase/tier(ops)/version — present.
- [x] Verb name, no repo/domain repetition — `smoke-test`.
- [x] What+when description — supplied and stamped.
- [x] No token invented — none used; scaffolder takes `--domain` only when given.

## Eval 2 — "/create — I want a new skill" (elicitation)
- [x] Nothing created — no scaffold before the brief.
- [x] Asks what + when — step 1; step 0 now also asks where it lives (existing vs new collection).
- [x] Asks action/knowledge + tier — step 1.
- [x] Asks phase — step 1.
- [x] No fabricated name/tier/token — workflow defers all three.

## Eval 3 — "MyDeploy_Helper_v2" (naming enforcement)
- [x] Non-compliance identified (case, underscores, version-in-name) — per §2; scaffolder also
  rejects it mechanically (`validate_name`).
- [x] Compliant alternative proposed — e.g. `deploy-helper`.
- [x] Version belongs in metadata.skillroy.version — stated (the rule eval 3 originally surfaced).
- [x] No folder created with the bad name — scaffold only after agreement.

## Eval 4 — Unknown token `frobnicator`
- [x] Checked against the catalog, not accepted blindly — review/--tokens path.
- [x] Reported as not a known canonical token.
- [x] Asks how to proceed (register as proposed / pick existing) — never invent.
- [x] No scaffold finalized with an unvetted token unflagged.

## Eval 5 — New project + first skill in one flow (NEW)
- [x] Compliant <tier>-<domain> name proposed — `dx-widgets` (not the user's wording).
- [x] Collection shell complete — README metadata block with `status: experimental`, `.claude/skills/`
  home, safe .gitignore (`.claude/*` + `!.claude/skills/`), .gitattributes, `.agents/skills` symlink.
- [x] First skill inside the new home, inheriting tier dx — `reshape`, `tier: dx`; `--license`
  propagated to the skill.
- [x] git init offered, not run — script prints the next-step note (git-free by design).
- [x] Domain token surfaced, not asserted — 'widgets' is not in the org catalog; chat door records it
  as a proposal for the catalog owner (the `tlog` precedent), or proceeds domain-less.

## Eval 6 — "Widget_Utils_Tools" (non-compliant collection name) (NEW)
- [x] Non-compliance called out — script: case/underscores rejected; kebab variant still rejected for
  the redundant `-tools` suffix; chat door raises the missing tier.
- [x] Compliant <tier>-<domain> alternative proposed, tier elicited.
- [x] Nothing created under the bad name — verified on disk.
- [x] `--brand` mentioned only as an explicit exception — per SKILL.md step 0.

Post-run: lint at publish clean; `skills-ref validate` → Valid (re-run after the SKILL.md change).
