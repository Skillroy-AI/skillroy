# Eval run — create v0.3.0 (publish)
- **Date / runner:** 2026-06-11 / claude-fable-5 (fresh subagent, no authoring context; simulated-interactive per CONVENTIONS §8)
- **Inputs:** no token catalog (intentional — every token expectation exercises the surface-never-invent ladder: `--tokens` absent → `$SKILLROY_TOKENS` unset → no catalog file); sandbox `/tmp/create-eval-run-s4/` with a git-initialized `work-repo/` as the invoking repo; simulated user replies noted per eval. Trigger for this run: v0.2.0→v0.3.0 (§0 destination-elicitation amendment + new expectations on evals 2 and 5).
- **Result:** PASS (6/6 evals, 30/30 expectations)

## Eval 1 — Happy path: ops action skill scaffolded with compliant name
- [x] Folder scaffolded with SKILL.md, references/, evals/ stub — `find` shows SKILL.md, references/.gitkeep, evals/evals.json under work-repo/.claude/skills/smoke-test/
- [x] Frontmatter `name` equals folder name, kebab-case — `name: smoke-test` == folder `smoke-test` (cat of SKILL.md)
- [x] metadata.skillroy block with phase, tier ops, version — frontmatter has `phase: adhoc`, `tier: ops`, `version: 0.1.0`
- [x] Name is verb-phrase, no 'skill'/repo/domain repeat — `smoke-test` proposed with alternative `check-deploy`; neither repeats repo/domain
- [x] Description is what + when with trigger phrases — "Run the project's smoke-test suite… Use after a deployment… when asked to smoke-test, sanity-check, or check a deploy"
- [x] No token invented; asks if one is needed — no `domain:` key in frontmatter; agent surfaced empty catalog ladder ($SKILLROY_TOKENS unset, no catalog file) and left domain unset, inviting registration
- Simulated user replies: "Yes — smoke-test, adhoc, this repo. Go." (confirming the brief incl. proposed phase and in-repo destination)

## Eval 2 — Under-specified /create: full elicitation, nothing created
- [x] No skill folder or files created this turn — `git status --porcelain` empty; sandbox root unchanged (only work-repo)
- [x] Asks the what + when (triggers) — elicitation item 1 asks what it does and what trigger conditions/phrases invoke it
- [x] Asks action-vs-knowledge and the tier — items 2–3 ask verb/noun kind and offer the kb/ops/int/dx/core/meta menu
- [x] Asks for the phase — item 4 asks brainstorming/adhoc/publish, noting it sets the bar
- [x] Establishes where it will live, not silently chosen — item 5 asks this repo vs existing vs new collection + parent dir: "that destination is your call"
- [x] No name, tier, or token fabricated — no name proposed; tier given only as the valid-values menu; token explicitly "won't invent one"
- Simulated user replies: none (turn ends waiting for the user)

## Eval 3 — Non-compliant skill name MyDeploy_Helper_v2 flagged, not scaffolded
- [x] Identifies non-compliance: uppercase, underscores, version in name — agent's turn calls out all three counts explicitly
- [x] Proposes compliant kebab-case alternative — proposed `deploy-helper` (alt `assist-deploy`)
- [x] Notes version belongs in metadata.skillroy.version, not the name — stated verbatim with the "names are API contracts" rationale
- [x] No folder literally named MyDeploy_Helper_v2 created — `find /tmp/create-eval-run-s4 -iname '*mydeploy*'` returns nothing; git clean
- Simulated user replies: none (turn ends awaiting name choice + missing brief items)

## Eval 4 — Unvetted token `frobnicator`: surfaced and queried, never baked in
- [x] Checks token against the catalog, not accepted blindly — agent ran the ladder: `printenv SKILLROY_TOKENS` (unset), catalog-file search and grep (none found)
- [x] Reports frobnicator is not a known canonical token — turn states "frobnicator is not a known canonical token — I can't validate it"
- [x] Asks how to proceed instead of inventing — offers (a) register in org catalog, (b) supply --tokens/$SKILLROY_TOKENS, (c) existing token, (d) scaffold with domain unset + TODO
- [x] No scaffold finalized with the unvetted token — nothing scaffolded; `grep -rl frobnicator` across sandbox returns nothing; git clean
- Simulated user replies: none (turn ends waiting on token decision and remaining brief)

## Eval 5 — New collection dx-widgets + first skill, parent decided by user with sign-off
- [x] Compliant <tier>-<domain> name proposed, not user's words — `dx-widgets` proposed; "widget data utilities" flagged as non-compliant verbatim
- [x] Parent location decided by user in the question itself; outside-repo write got explicit sign-off — turn 1 was the question (3 parent options + recommendation + rationale + sign-off request); zero writes before the user's reply chose (b) with explicit OK
- [x] Shell scaffolded: README block status: experimental, .claude/skills/, safe .gitignore, .agents symlink — cat README shows `status: experimental` block; .gitignore has `.claude/*` + `!.claude/skills/`; `readlink .agents/skills` → `../.claude/skills`; git ls-files proves skills tracked
- [x] First skill inside new home, inherits collection tier — dx-widgets/.claude/skills/reshape-exports/SKILL.md has `tier: dx`
- [x] git init offered as follow-up, not run by scaffolder — `ls -a` after scaffold showed no `.git`; script printed "next: … git init … (scripts are git-free by design)"; init ran only after user accepted
- [x] Domain token surfaced as needing a catalog, not silently asserted — agent flagged no catalog on the ladder and asked; skill frontmatter has no `domain:` key
- Simulated user replies: "Go with (b) — /tmp/create-eval-run-s4/dx-widgets. Explicit OK to write outside work-repo."; "Leave domain unset until we have a catalog; reshape-exports is good — go."; "Yes, init it."

## Eval 6 — Non-compliant collection name Widget_Utils_Tools rejected, tier elicited
- [x] Identifies case, underscores, redundant -Tools suffix, and missing tier — agent's turn enumerates all four counts against §2
- [x] Compliant <tier>-<domain> alternative proposed, tier elicited — proposes `dx-widgets` / `ops-widgets` while asking "which tier?"
- [x] No folder named Widget_Utils_Tools created — driver-side check: new-collection.py rejects it ("error: invalid name … lowercase kebab-case [a-z0-9-] only", exit 2); scratch empty; sandbox-wide find empty
- [x] --brand mentioned only as explicit, deliberate exception — described as "a deliberate call you'd make, not a default I'll apply"; never used
- Simulated user replies: none (turn ends awaiting tier/name decision)

## Run notes
1. §0 gap surfaced by eval 1: the invoking repo was a plain repo, not yet a collection (no README
   metadata block); SKILL.md's "existing collection → scaffold into its .claude/skills/" doesn't
   cover that middle case. The runner interpreted in-repo scaffolding (creating the home dir en
   route) as correct for a single skill, with the destination confirmed in the brief — a clarifying
   line in §0 would remove the ambiguity. (Minor; future wording pass, no behavior change.)
2. The amended §0 worked as intended in eval 5: framing the parent location as the question itself
   (not a preamble) made the explicit-sign-off-before-outside-write gate natural to honor; no writes
   happened pre-reply.
3. `new-collection.py --with-skill` exposes no `--phase`/`--description`; the first skill lands at
   `phase: brainstorming` with a TODO description — fine, but the inherited defaults are worth
   documenting. It also creates `.gitattributes`, matching eval 5's expected_output prose though
   absent from its expectations list.
4. Driver bookkeeping: eval-1 output was committed in work-repo so evals 2–4's no-write checks had a
   clean `git status` baseline; eval-6's script-rejection run was driver-side verification in
   `scratch/` (the conversational agent never invoked the scaffolder with the bad name).
5. Evidence left in place: `/tmp/create-eval-run-s4/` (work-repo with smoke-test committed;
   dx-widgets git-initialized; empty scratch/).
