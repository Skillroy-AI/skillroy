# Eval run — migrate v0.1.0 (run at adhoc; promoted to publish on pass)
- **Date / runner:** 2026-06-09 / Claude Fable 5 (Claude Code session)
- **Inputs:** fixture repo Legacy-Helpers (3 uninstrumented skills under .agents/skills/, license
  present, `.claude/` gitignored, non-kebab repo name) + destination dx-helpers. Eval 3's user turn
  simulated by the eval driver per CONVENTIONS §8. Supplementary real-world evidence: the
  Tlog-Tools → dx-tlog migration run the same day (see Post-run).
- **Result:** PASS (4/4 evals, 16/16 expectations)

## Eval 1 — "What would migrating ./Legacy-Helpers involve?"
- [x] Read-only plan run against the source — `migrate-skill.py plan Legacy-Helpers`.
- [x] Report covers homes, uninstrumented skills, missing evals, gitignore trap — all four in the plan output.
- [x] Tier / domain tokens / repo rename posed as the user's decisions — printed under "decisions needed (yours, not the script's)".
- [x] Nothing modified — no `.claude/` created; fixture tree unchanged.

## Eval 2 — Apply into ./dx-helpers (ratified: tier dx; lookup=helper-core; others domain-less)
- [x] Source untouched — all 3 source SKILL.mds still lack `skillroy:`; no `.claude/` in source.
- [x] Metadata injected with ratified values; domain only on lookup — grep: lookup 1 domain line, others 0.
- [x] Frontmatter/body byte-preserved — original body line intact ("must survive migration byte-for-byte").
- [x] Destination .gitignore patched + .agents/skills symlink created — `.claude/*` + `!.claude/skills/`; link → `../.claude/skills`.
- [x] Verified with review's linter; publish requires an eval run stated — lint: instrumented, only info-level "no evals" at adhoc; SKILL.md §4/§6 reiterate the publish rule.
- [x] Phase adhoc; publish refused — `apply --phase publish` exits with "publish is earned by an eval run" (also covered by `--self-test`).

## Eval 3 — Made-up domain token 'parsetron'
- [x] Not injected — no `--domain` passed for it; skill declines per §3.
- [x] Explains tokens come only from the canonical catalog — per SKILL.md §3 / CONVENTIONS §5.
- [x] Offers the compliant path — propose in the overlay catalog (status: proposed, SME ratifies) or migrate domain-less. (Exercised for real earlier with the `tlog` token: proposed in the dry-run → SME-confirmed → only then used.)

## Eval 4 — Re-run the migration
- [x] No duplication — skills reported "destination already exists — left as-is"; still exactly one `skillroy:` block per SKILL.md.
- [x] Already-completed steps reported as such — gitignore/symlink actions not repeated.
- [x] Report distinguishes already-done from newly-done — apply output per item.

Post-run (real-world evidence): migrated Tlog-Tools → ~/Projects/dx-tlog the same day with this
skill — clone preserving history, plan → ratified inputs (tier dx, domain tlog ×2, xslt domain-less,
phase adhoc) → in-place apply (move + symlink + gitignore patch + injection), ratified evals
imported, lint clean against the org catalog (provenance-stamped), `mvn validate` clean after the
identity commit. Structural lint at publish clean; `skills-ref validate` → Valid.
